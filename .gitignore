# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo

# Environment
.env
.env.*

# Node (if modules are ever added)
node_modules/

# Instance-specific files (these belong to individual installs, not the product repo)
# Uncomment if you ever test MLS Core locally inside this repo:
# .mls/metrics.json
# .mls/context/CHANGELOG_ARCHIVE.md
